# 지도 공통 설정

## 기본 색상

| 구분 | 색상 |
|---|---|
| 일반 도심 | `#E0E3E6` |
| 동네 도로 | `#F0F0F0` |
| 큰 도로 | `#F0D2AA` |
| 바다·강 | `#A4CFEA` |
| 산·공원 | `#D2E1C8` |

모든 여행지 지도의 최초 제작값으로 사용. 여행지별로 다른 값이 확정된 경우 해당 여행지의 `trip_settings.md`에 별도 기록.

## 실행용 지도 방식

- 고해상도 정적 지도 한 장 사용
- 지도 색상은 이미지 제작 단계에서 미리 적용
- 브라우저에서 여러 고해상도 마스크를 겹치지 않음
- 확대·축소·드래그는 SVG `viewBox`만 변경
- 장소 표시와 이동 경로는 SVG 요소로 지도 위에 배치

## 임시 색상 설정창

사용자가 색상 조정을 요청한 기간에만 사용.

1. 원본 지도를 일반 도심, 동네 도로, 큰 도로, 물, 녹지로 분리
2. 일반 도심은 SVG 배경색 사용
3. 나머지 네 항목은 흑백 WebP 마스크로 분리
4. 지도 오른쪽에 다섯 개의 색상 입력과 현재 설정값 복사 버튼 표시
5. 색상 입력 변경 시 해당 SVG 레이어 색상 즉시 변경
6. 복사값에 지도 중심, 배율, `viewBox`, 다섯 색상 포함

포르투갈 프로젝트의 `tools/stylize_seoul_map.py --masks-only`로 임시 마스크 생성. 색상 확정 후 마스크 파일 삭제.

## 색상 확정 절차

1. 선택한 색상을 제작 스크립트의 기본 팔레트에 기록
2. 고해상도 단일 WebP 다시 제작
3. HTML의 분리 마스크와 색상 설정창 제거
4. 단일 WebP만 표시
5. 오른쪽 위 버튼을 화면 값 복사 기능으로 복원

색상 설정창을 다시 열어달라는 요청이 있으면 위 임시 방식으로 전환. 확정 후 다시 단일 이미지 방식으로 복귀.
